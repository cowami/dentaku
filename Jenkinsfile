pipeline {
    agent any

    stages {
        stage('Debug') {
            steps {
                sh 'echo "Hello, Jenkins!"'
                sh 'whoami'
                sh 'pwd'
            }
        }
    }
}